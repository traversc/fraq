% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_downsample}
\alias{fraq_downsample}
\title{Downsample FASTQ file(s)}
\usage{
fraq_downsample(input, output, amount, nthreads = 1L)
}
\arguments{
\item{input}{Character vector of one or more input FASTQ file paths.
Vectors must be the same length as \code{output} (e.g., R1 and R2 pairs).}

\item{output}{Character vector of output FASTQ file paths, same length
as \code{input}.}

\item{amount}{Numeric scalar in (0, 1); proportion of reads to retain.}

\item{nthreads}{Integer number of threads.}
}
\value{
Invisibly returns \code{NULL} after writing the downsampled outputs.
}
\description{
Write deterministically downsampled FASTQ file(s) to disk.
\code{input} and \code{output} must be vectors of the same length (e.g., R1/R2 pairs).
}
\details{
Downsampling is deterministic: given the same inputs, \code{fraq_downsample()}
keeps the same records every run while matching the requested proportion
as closely as possible.
}
\examples{
r1 <- tempfile(fileext = ".fastq")
r2 <- tempfile(fileext = ".fastq")
generate_random_fastq(r1, n_reads = 1000, read_length = 100, 
    name_prefix = "read_R1_")
generate_random_fastq(r2, n_reads = 1000, read_length = 100, 
    name_prefix = "read_R2_")
out <- c(tempfile(fileext = ".fastq"), tempfile(fileext = ".fastq"))
fraq_downsample(c(r1, r2), out, amount = 0.1)
}

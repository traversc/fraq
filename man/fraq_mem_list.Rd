% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fraq_mem.R
\name{fraq_mem_list}
\alias{fraq_mem_list}
\alias{fraq_mem_remove}
\alias{fraq_mem_load}
\title{Manage in-memory FASTQ datasets}
\usage{
fraq_mem_list()

fraq_mem_remove(mem_key)

fraq_mem_load(input, mem_key, nthreads = 1L)
}
\arguments{
\item{mem_key}{Character vector of \code{.mem} keys to remove.}

\item{input}{Character vector of FASTQ/FRAQ paths to load into memory.}

\item{nthreads}{Positive integer parallelism for the load.}
}
\value{
\itemize{
\item \code{fraq_mem_list()} returns a data frame with columns \code{mem_key} and
\code{n_reads}.
\item \code{fraq_mem_remove()} returns a logical vector indicating which keys were
removed.
\item \code{fraq_mem_load()} returns the target \code{.mem} keys invisibly, exactly as
supplied.
}
}
\description{
\code{fraq_mem_list()} summarizes the \code{.mem} datasets currently stored in the
session. \code{fraq_mem_remove()} deletes one or more \code{.mem} entries, freeing the
associated memory. \code{fraq_mem_load()} is a convenience wrapper around
\code{\link[=fraq_convert]{fraq_convert()}} that loads on-disk FASTQ/FRAQ inputs into the in-memory
store after validating that the outputs end with \code{.mem}.

The in-memory store lives in the current R session. For consistent results,
call the helper functions when no other \code{fraq} jobs are actively writing to
the same \code{.mem} keys.
}
\examples{
tmp <- tempfile(fileext = ".fastq")
generate_random_fastq(tmp, n_reads = 100, read_length = 75)
mem_path <- tempfile(fileext = ".mem")
fraq_mem_load(tmp, mem_path)
fraq_mem_list()
fraq_mem_remove(mem_path)
}

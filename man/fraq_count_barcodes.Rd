% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_count_barcodes}
\alias{fraq_count_barcodes}
\title{Count barcodes in FASTQ file(s)}
\usage{
fraq_count_barcodes(
  input,
  barcodes,
  max_distance = 1L,
  allow_revcomp = FALSE,
  nthreads = 1L
)
}
\arguments{
\item{input}{Character vector of one or more input FASTQ file paths (e.g.,
R1 and R2).}

\item{barcodes}{Character vector of barcode sequences to count.}

\item{max_distance}{Integer maximum number of mismatches allowed for a match.}

\item{allow_revcomp}{Logical; if TRUE, also match reverse complements.}

\item{nthreads}{Integer number of threads.}
}
\value{
A data frame with counts per barcode.
}
\description{
Count occurrences of provided short barcodes in reads,
allowing up to \code{max_distance} mismatches. Accepts one or more FASTQ files
(e.g., R1/R2).
}
\examples{
r1 <- tempfile(fileext = ".fastq")
r2 <- tempfile(fileext = ".fastq")
generate_random_fastq(r1, n_reads = 1000, read_length = 100, 
    name_prefix = "read_R1_")
generate_random_fastq(r2, n_reads = 1000, read_length = 100, 
    name_prefix = "read_R2_")
short_barcodes <- c("ACGT", "TGCA", "GTAC")
counts <- fraq_count_barcodes(c(r1, r2), short_barcodes, max_distance = 1L, 
    allow_revcomp = FALSE, nthreads = 1L)
counts
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_merge_pairs}
\alias{fraq_merge_pairs}
\title{Merge paired-end reads into a consensus}
\usage{
fraq_merge_pairs(
  input,
  output_merged,
  output_unmerged = NULL,
  min_overlap = 12L,
  max_mismatch_rate = 0.1,
  consensus_mode = c("max", "mean", "r1", "r2"),
  trim_overhang = TRUE,
  revcomp_R2 = TRUE,
  nthreads = 1L
)
}
\arguments{
\item{input}{Character vector of length 2 with input FASTQ paths (R1, R2).}

\item{output_merged}{Character scalar path/key receiving merged single-end
reads.}

\item{output_unmerged}{Optional character vector of length 2 for unmerged
R1/R2 outputs. Use \code{NULL} to drop unmerged pairs.}

\item{min_overlap}{Integer minimum overlap required to attempt merging.}

\item{max_mismatch_rate}{Numeric maximum mismatch fraction allowed within
the overlap.}

\item{consensus_mode}{Character string controlling consensus base
selection: \code{"max"}, \code{"mean"}, \code{"r1"}, or \code{"r2"}.}

\item{trim_overhang}{Logical; if \code{TRUE}, include non-overlapping tails when
constructing the merged read.}

\item{revcomp_R2}{Logical; if \code{TRUE}, reverse-complement R2 before merging.}

\item{nthreads}{Integer number of worker threads.}
}
\value{
A list summarising merge statistics (\code{merged_reads},
\code{unmerged_reads}, \code{mean_insert_size}, etc.).
}
\description{
Merge R1/R2 pairs by overlapping sequences (optionally
reverse-complementing R2), emitting merged reads and optional unmerged outputs.
}
\details{
Qualities are interpreted as PHRED+33.
}
\examples{
r1 <- tempfile(fileext = ".fastq")
r2 <- tempfile(fileext = ".fastq")
generate_random_fastq(r1, n_reads = 100, read_length = 100, 
    name_prefix = "read_R1_")
generate_random_fastq(r2, n_reads = 100, read_length = 100, 
    name_prefix = "read_R2_")
out_merged <- tempfile(fileext = ".fastq")
fraq_merge_pairs(c(r1, r2), out_merged, output_unmerged = NULL, 
    min_overlap = 20L, max_mismatch_rate = 0.05)
}

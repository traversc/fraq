% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_convert}
\alias{fraq_convert}
\title{Convert sequencing files between supported formats}
\usage{
fraq_convert(input, output, nthreads = 1L)
}
\arguments{
\item{input}{Character vector of source files/keys.}

\item{output}{Character vector of destination files/keys, same length
as \code{input}.}

\item{nthreads}{Integer number of threads for reading/writing.}
}
\value{
Invisibly returns \code{NULL} after writing the converted outputs.
}
\description{
Re-encode sequencing files in any supported FRAQ/FASTQ format.
Input and output vectors must be the same length.
}
\details{
FIFO pipes (paths ending with \code{.fifo}) are only available on Unix-like
systems;
on Windows they are not supported and will trigger an error.
}
\examples{
src <- tempfile(fileext = ".fastq")
generate_random_fastq(src, n_reads = 10, read_length = 50)
dest <- tempfile(fileext = ".fastq.gz")
fraq_convert(src, dest, nthreads = 1L)
}

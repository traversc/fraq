% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_demux}
\alias{fraq_demux}
\title{Demultiplex FASTQ file(s) by barcode prefix}
\usage{
fraq_demux(input, output_format, barcodes, max_distance = 1L, nthreads = 1L)
}
\arguments{
\item{input}{Character vector of one or more input FASTQ file paths
(e.g., R1 and R2).}

\item{output_format}{Character vector of format strings, same length
as \code{input}. Each string must contain the literal \code{{barcode}} placeholder.}

\item{barcodes}{Character vector of barcode sequences to test as prefixes.}

\item{max_distance}{Integer maximum Hamming distance allowed between
barcode and read prefix.}

\item{nthreads}{Integer number of threads.}
}
\value{
Invisibly, \code{NULL}. Files are written to disk according to
\code{output_format}.
}
\description{
Write barcode-specific outputs by matching a prefix on the
first read of each record. Each output path is formed by substituting
\code{{barcode}} in the supplied format string.
}
\details{
When no barcode matches, the literal \code{NO_MATCH} is substituted in place of
\code{{barcode}}. If multiple barcodes match the same read, \code{MULTI_MATCH} is used.
}
\examples{
r1 <- tempfile(fileext = ".fastq")
generate_random_fastq(r1, n_reads = 1000, read_length = 100, 
    name_prefix = "read_R1_")
out <- tempfile("R1_", fileext = "_{barcode}.fastq")
barcodes <- c("ACGT", "TGCA", "GTAC")
fraq_demux(r1, out, barcodes, max_distance = 1L, nthreads = 1L)
}

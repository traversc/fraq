% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_slice}
\alias{fraq_slice}
\title{Slice reads by index or limit}
\usage{
fraq_slice(input, output, limit = NULL, select = NULL, nthreads = 1L)
}
\arguments{
\item{input}{Character vector of source files/keys.}

\item{output}{Character vector of destination files/keys, same length as
\code{input}.}

\item{limit}{Numeric scalar; keep the first \code{limit} reads (per record index).
Defaults to \code{NULL}.}

\item{select}{Numeric vector of zero-based indices to keep. Defaults to
\code{NULL}.}

\item{nthreads}{Integer number of threads for reading/writing.}
}
\value{
Invisibly returns \code{NULL} after writing the selected reads.
}
\description{
Write a subset of reads from \code{input} to \code{output}, either the
first \code{limit} reads or specific zero-based indices in \code{select}.
}
\details{
Exactly one of \code{limit} or \code{select} must be supplied.
}
\examples{
src <- tempfile(fileext = ".fastq")
generate_random_fastq(src, n_reads = 10, read_length = 50)
dest <- tempfile(fileext = ".fastq")
fraq_slice(src, dest, limit = 5)
}

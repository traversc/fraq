% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_quality_filter}
\alias{fraq_quality_filter}
\title{Filter reads by whole-read quality}
\usage{
fraq_quality_filter(
  input,
  output,
  min_mean_quality = 20,
  max_low_q_bases = .Machine$integer.max,
  low_q_threshold = 20L,
  nthreads = 1L
)
}
\arguments{
\item{input}{Character vector of one or more input FASTQ file paths. Must
be the same length as \code{output}.}

\item{output}{Character vector of output FASTQ paths.}

\item{min_mean_quality}{Numeric minimum mean base quality required for
each mate.}

\item{max_low_q_bases}{Integer maximum number of bases below
\code{low_q_threshold} allowed per mate.}

\item{low_q_threshold}{Integer PHRED cutoff used to count low-quality bases.}

\item{nthreads}{Integer number of worker threads.}
}
\value{
Invisibly, \code{NULL}. Reads are written to \code{output} paths for records
that pass the filters.
}
\description{
Drop read sets when any mate fails the quality thresholds.
Qualities are interpreted as PHRED+33.
}
\details{
Both thresholds are evaluated separately on every mate. If any mate fails,
the entire read set is discarded.
}
\examples{
r1 <- tempfile(fileext = ".fastq")
generate_random_fastq(r1, n_reads = 1000, read_length = 100, 
    name_prefix = "read_R1_")
out <- tempfile(fileext = ".fastq")
fraq_quality_filter(r1, out, min_mean_quality = 25, max_low_q_bases = 2L, 
    low_q_threshold = 20L, nthreads = 1L)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_chunk}
\alias{fraq_chunk}
\title{Chunk sequencing files into fixed-size batches}
\usage{
fraq_chunk(input, output_prefix, output_suffix, chunk_size, nthreads = 1L)
}
\arguments{
\item{input}{Character vector of source files/keys.}

\item{output_prefix}{Character vector of prefixes used when naming chunked
outputs. Must be the same length as \code{input}.}

\item{output_suffix}{Character scalar describing the output format; use
\code{"fastq"}, \code{"fraq"}, \code{"mem"}, \code{"gz"}, \code{"zst"}.}

\item{chunk_size}{Numeric chunk size in reads; each output chunk contains
up to this many records (per input stream).}

\item{nthreads}{Integer number of worker threads.}
}
\value{
Invisibly returns \code{NULL} after writing all chunked outputs.
}
\description{
Split input datasets into sequential chunks. Each chunk is
written using \code{output_prefix} suffixed with \verb{_chunk\{N\}} and the format
indicated by \code{output_suffix}.
}
\details{
The suffix mapping follows:
\itemize{
\item \code{"fastq"} -> \code{.fastq}
\item \code{"gz"} -> \code{.fastq.gz}
\item \code{"zst"} -> \code{.fastq.zst}
\item \code{"fraq"} -> \code{.fraq}
\item \code{"mem"} -> \code{.mem}
}
}
\examples{
r1 <- tempfile(fileext = ".fastq")
generate_random_fastq(r1, n_reads = 25, read_length = 75)
fraq_chunk(r1,
           output_prefix = tempfile("chunked_R1"),
           output_suffix = "fastq",
           chunk_size = 10,
           nthreads = 1L)
}

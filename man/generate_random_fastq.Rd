% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_random_fastq.R
\name{generate_random_fastq}
\alias{generate_random_fastq}
\title{Generate a random FASTQ file (optionally gzipped)}
\usage{
generate_random_fastq(
  output_file,
  n_reads = 1e+05,
  read_length = 100,
  name_prefix = "read_"
)
}
\arguments{
\item{output_file}{Character vector of length 1 (single-end) or 2 (paired-end)
outputs. \code{.gz} suffixes create gzip-compressed files; otherwise plain-text
FASTQ is written.}

\item{n_reads}{Integer number of reads to generate. Default \code{1e5}.}

\item{read_length}{Integer read length (number of bases per read). Default \code{100}.}

\item{name_prefix}{Character prefix for read names. Default \code{"read_"}.}
}
\value{
Invisibly returns the path(s) written in \code{output_file}.
}
\description{
Creates a synthetic FASTQ file with random DNA sequences and Illumina-like
Phred+33 quality strings (high early-cycle quality with a gentle tail drop).
If \code{output_file} ends with \code{.gz}, the file is written
gzip-compressed via a connection.
}
\details{
Each read comprises four lines: header, sequence, \code{+}, and quality.
Headers are generated as \verb{@<name_prefix><index>}. Sequences are sampled uniformly
from \code{ACGT}. Qualities follow a tapered profile that starts near Q37 and falls
toward the low 30s, with occasional low-quality spikes to mimic typical
Illumina output.
}
\examples{
# Example: small test files
tmp_fastq <- tempfile(fileext = ".fastq")
tmp_fastq_gz <- tempfile(fileext = ".fastq.gz")

# Create plain FASTQ (500 reads, length 100)
generate_random_fastq(tmp_fastq, n_reads = 500, read_length = 100)

# Create gzipped FASTQ (500 reads, length 100)
generate_random_fastq(tmp_fastq_gz, n_reads = 500, read_length = 100)

# Paired-end example with overlapping mates
generate_random_fastq(c(tmp_fastq, tmp_fastq_gz),
                      n_reads = 100,
                      read_length = 150)
}

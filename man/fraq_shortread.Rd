% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fraq_shortread.R
\name{fraq_shortread}
\alias{fraq_shortread}
\alias{fraq_export_shortreadq}
\alias{fraq_import_shortreadq}
\title{Bridge FRAQ formats with ShortReadQ}
\usage{
fraq_export_shortreadq(input, nthreads = 1L, tmpdir = tempdir())

fraq_import_shortreadq(shortreadq, output, nthreads = 1L, tmpdir = tempdir())
}
\arguments{
\item{input}{Character vector of input paths/keys accepted by
\code{\link[=fraq_convert]{fraq_convert()}}.}

\item{nthreads}{Positive integer passed to \code{fraq_convert}.}

\item{tmpdir}{Directory used for staging temporary files.}

\item{shortreadq}{A \code{ShortReadQ} or list of \code{ShortReadQ} objects to be
written via FRAQ encoders.}

\item{output}{Character vector of destination paths/keys, same length as
\code{shortreadq}.}
}
\value{
\itemize{
\item \code{fraq_export_shortreadq()} returns a single \code{ShortReadQ} when \code{input} has
length 1, otherwise a list of \code{ShortReadQ} objects.
\item \code{fraq_import_shortreadq()} invisibly returns the normalized \code{output}
vector after conversion.
}
}
\description{
\code{fraq_export_shortreadq()} converts FRAQ/FASTQ inputs into in-memory
\code{ShortReadQ} objects via \code{fraq_convert}. \code{fraq_import_shortreadq}
converts \code{ShortReadQ} objects to any supported FRAQ format.
}
\examples{
if (requireNamespace("ShortRead", quietly = TRUE)) {
fq <- tempfile(fileext = ".fastq")
generate_random_fastq(fq, n_reads = 10, read_length = 50)
fraq_path <- tempfile(fileext = ".fraq")
fraq_convert(fq, fraq_path)

reads <- fraq_export_shortreadq(fraq_path)
roundtrip_fastq <- tempfile(fileext = ".fastq")
fraq_import_shortreadq(reads, roundtrip_fastq)
stopifnot(file.exists(roundtrip_fastq))
}

}
\seealso{
\code{ShortRead::readFastq()}, \code{ShortRead::writeFastq()},
\code{\link[=fraq_convert]{fraq_convert()}}
}

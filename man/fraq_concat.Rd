% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_documentation.R
\name{fraq_concat}
\alias{fraq_concat}
\title{Concatenate sequencing files}
\usage{
fraq_concat(input, output, nthreads = 1L)
}
\arguments{
\item{input}{Character vector of input paths/keys to concatenate. Mixed
formats are supported.}

\item{output}{Character scalar giving the destination path (or \code{.mem} key).}

\item{nthreads}{Integer number of threads for reading, compression, and
writing.}
}
\value{
Invisibly returns \code{NULL} after writing the concatenated output.
}
\description{
Concatenate one or more FRAQ/FASTQ inputs (plain,
\code{.gz}, \code{.zst}, \code{.fraq}, or \code{.mem}) into a single output file.
}
\examples{
tmp_dir <- tempdir()
inputs <- file.path(tmp_dir, sprintf("reads_\%d.fastq", 1:2))
lapply(inputs, generate_random_fastq, n_reads = 10, read_length = 50)
out <- file.path(tmp_dir, "all_reads.fastq.gz")
fraq_concat(inputs, out, nthreads = 1L)
}
